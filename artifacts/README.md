# Checkpoints

Put the four `.pth` files in `checkpoints/`:

- `convnext-small.pth`
- `densenet121.pth`
- `mobilenet-v2.pth`
- `efficientnet-b1.pth`

Weights are gitignored. The UI dropdown shows whichever files are present.
